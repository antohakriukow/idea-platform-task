import { COLOR_GRAY_300, COLOR_GRAY_500 } from '../styles/colors';

export enum Size {
  L = 'Large',
  M = 'Medium',
  S = 'Small',
}

export enum TextColor {
  DEFAULT = COLOR_GRAY_500,
  LIGHT = COLOR_GRAY_300,
  WHITE = '#fff',
}
